# Парсер для восстановление сайта журнала Пропаганда (propaganda-journal.net)

Запуск парсера

```
yarn install
node ./index.js
```

Парсер ожидает, что входные данные будут в каталоге `site`, а выходные в каталоге `site_out`.